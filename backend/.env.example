# Backend Environment Variables for Render Deployment

# Server Configuration
PORT=8000
HOST=0.0.0.0

# CORS Configuration - Update with your actual Vercel domain
CORS_ORIGINS=https://your-app.vercel.app,http://localhost:5173

# Stock Data Directory (optional - uses /tmp on Render)
STOCK_DATA_DIR=/tmp/stock_data

# Python Path (for Render deployment)
PYTHONPATH=/app

# Optional: API Rate Limiting
API_RATE_LIMIT=100

# Optional: Cache TTL for ticker tape (seconds)
TICKER_CACHE_TTL=45